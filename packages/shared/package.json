{
    "name": "@database-gui/shared",
    "version": "1.0.0",
    "description": "Shared utilities and constants for web client and authentication",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "type": "module",
    "scripts": {
        "type-check": "tsc --noEmit",
        "build": "tsc",
        "clean": "rm -rf dist",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "@database-gui/types": "1.0.0"
    }
}