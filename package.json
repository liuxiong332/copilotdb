{
    "name": "database-gui-client",
    "version": "1.0.0",
    "description": "Multi-platform database GUI client with AI assistance",
    "private": true,
    "workspaces": [
        "packages/*",
        "apps/*"
    ],
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build",
        "test": "turbo run test",
        "lint": "turbo run lint",
        "type-check": "turbo run type-check",
        "clean": "turbo run clean"
    },
    "devDependencies": {
        "turbo": "^1.13.4",
        "typescript": "^5.9.2"
    },
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=8.0.0"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.55.0"
    }
}